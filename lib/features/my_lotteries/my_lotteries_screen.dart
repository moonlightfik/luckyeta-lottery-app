import 'package:flutter/material.dart';

class MyLotteriesScreen extends StatelessWidget {
  const MyLotteriesScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('MyLotteriesScreen placeholder'),
      ),
    );
  }
}
